package com.sardois.sgit

import org.scalatest._

class DiffSpec extends FlatSpec {

    "The diff algorithm" should "works" in {
        /*
        val l1 = Seq(
            "A",
            "B",
            "C"
        )
        val l2 = Seq(
            "B",
            "C"
        )

        val l3 = Diff.linesFromString(l1)
        val l4 = Diff.linesFromString(l2)
        println(Diff.diff(l3, l4))
         */
    }
}